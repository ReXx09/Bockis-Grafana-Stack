# Lokaler AIO-Manager-Test, nicht fuer die Unraid-XML benoetigt.
# Die Unraid-XML setzt dieselben Werte als Container-Umgebungsvariablen.

AIO_HOST_DATA_DIR=./aio-data
AIO_GRAFANA_PORT=3000
AIO_INFLUXDB_PORT=8086
AIO_SYSLOG_PORT=5514
AIO_MANAGER_PORT=8800

# Nur fuer einen lokalen Testlauf setzen; echte Geheimnisse nicht committen.
# Der Setup-Assistent erzeugt den Influx-Admin-Token selbst.